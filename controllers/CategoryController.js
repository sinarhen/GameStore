// Get all categories
const getAllCategories = () => {
    // Your code to fetch all categories from the database or any other data source
};

// Get category by id
const getCategoryById = (categoryId) => {
    // Your code to fetch a category by its id from the database or any other data source
};

module.exports = {
    getAllCategories,
    getCategoryById,
};
